/// SmartFarm XR Uygulama Enum'ları
/// Tüm enum'lar burada merkezi olarak tanımlanır
class AppEnums {
  // Private constructor - singleton pattern
  AppEnums._();
}

/// Uyarı Seviyesi Enum'u
enum UyariSeviyesi {
  info,
  warning,
  critical,
}
